# Database Configuration
# Uncomment and configure one of the following options

# Option 1: PostgreSQL (recommended for production)
DATABASE_URL=postgresql://username:password@localhost:5432/biopellet_trade

# Option 2: SQLite (simpler for local development)
# DATABASE_URL=sqlite:///pellet_trading.db

# Secret key for session management (change this to a secure random string)
SESSION_SECRET=change_this_to_a_secure_random_secret_key

# Flask Configuration
FLASK_APP=main.py
FLASK_ENV=development  # Change to 'production' for production deployments

# Optional: Debug mode (remove or set to False in production)
FLASK_DEBUG=True